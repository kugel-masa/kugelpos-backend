#
# < MongoDB >
#
# connection string
MONGODB_URI=your_mongodb_uri
# Database name
DB_NAME_PREFIX=your_database_name

# < Web Service >
#
# Base URL for the terminal service.
# account's own routes call no other service, but the request-logging middleware
# resolves this whenever a request carries an X-API-KEY header and a terminal_id.
# e.g. http://terminal.myserver.com/api/v1
BASE_URL_TERMINAL=http://localhost:8001/api/v1

# < Auth >
#
# Generate a secret key using the following command:
# openssl rand -hex 32
SECRET_KEY=your_secret_key 
# The algorithm used to sign the token
ALGORITHM=HS256
# The expiration time of the token in minutes
TOKEN_EXPIRE_MINUTES=30

# Other environment variables
# ...

# < Request body limit >
#
# Maximum request body size in bytes (issue #195). Enforced outermost, ahead of
# authentication, so an unauthenticated caller cannot decide how much memory
# this worker spends. Larger bodies are refused with 413.
MAX_REQUEST_BODY_BYTES=1048576
